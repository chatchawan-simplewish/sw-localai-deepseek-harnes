# VM105 candidate smoke: startup isolation readiness

Recorded 2026-09-09 00:25 Asia/Bangkok. Final read-only source verification: `2026-09-08T17:25:07.504792+00:00`, retained SSH exit `0`.

Repository: `https://github.com/chatchawan-simplewish/sw-localai-deepseek-harnes.git`, verified with `git remote get-url origin` in the authoritative-roadmap worktree. Author lane: `/root/candidate_smoke_review`. This lane wrote only this document and performed no installed-JavaScript execution, namespace creation, service operation, profile read, API request, browser action or inference. Other agents' files were preserved.

**Design verdict: READY FOR INDEPENDENT IMPLEMENTATION REVIEW with the constraints below. Runtime remains NOT PROVEN.** A second temporary candidate process does not replace the pilot; owner readiness for fresh credentials is not required for this bounded smoke. It does not authorize the production selector change or claim cutover acceptance. No evidence task is promoted by this document; full-plan progress remains 09/21.

## Evidence method and limits

Reused the in-memory `Inspection` definitions from `scripts/inspect-vm105-pnpm-package.py`, after checking SHA-256 `fcddc77c890b289e28c609fa30b3b41f6c060a73c5c4ea3ab69b468a130df424`. Source reads used held nofollow descriptors, trusted root ownership/modes, ACL/capability rejection, before/after metadata checks and double SHA-256 reads. Final validation also checked the three package logical links, identities, dependency declarations and all seven new file pins. Every SSH call returned 0. Transport: existing project key, BatchMode, IdentitiesOnly, StrictHostKeyChecking, ConnectTimeout=10, `dsh@192.168.1.139`, `/usr/bin/python3.12 -I`; each local SSH subprocess capped at25 seconds.

Seven additional public files were read, below the eight-file bound. Already recorded app-boot, root/web manifests and web bundle patch were reused/rechecked. The CLI manifest's `files: lib/*.js` includes the two exact statically imported boot chunks. No generic directory crawl or opaque profile inspection occurred. This is a bounded startup-chain review, not a complete audit of every transitive plugin. Namespace and filesystem controls supply containment beyond the inspected chain.

The root CLI and web package canonical/logical paths are those in `vm105-v1-source-revalidation-2026-09-09.json`. New webserver logical path is the web package's sibling `node_modules/@deepseek-ai/dsh-host-webserver`; its canonical root is:

`/opt/deepseek-harness/node_modules/.pnpm/@deepseek-ai+dsh-host-webserver@0.1.1-rc.2_@deepseek-ai+cordis@4.0.1_@deepseek-ai+dsh-i_58d11404f8dbf7a916bca46d37e27714/node_modules/@deepseek-ai/dsh-host-webserver`

| Package | Additional public file | SHA-256 |
| --- | --- | --- |
| dsh | lib/bin.js | c0226687bb20f45c603ec6fe50f3de16d1c3510c3a803304ec575ef9bc366c62 |
| dsh | lib/profile-boot-BnJoK_kl.js | 778c5b338674d986a49972be920c965d28b2c8cac85364ae77f8587070397663 |
| dsh | lib/profile-boot-DG5t9aNs.js | f83ffea6a4d30cfbe02b41dabcc05104c4ad27bf79c74f601f0ddb6ccdf88969 |
| dsh-web-app | lib/index.js | b824bfc687db3369cacb23a7a62bceecc936bd8b4c7a4b0bc6b50ad460bd0fda |
| dsh-web-app | lib/startup.js | 95a47053483fbe8ec86711369b8791bccb592c303fcc89300458a4bd07c6c252 |
| dsh-host-webserver | package.json | 163db0522737b595a6d4054252548960fb692b931071ca36bdd70a504ae1f35b |
| dsh-host-webserver | lib/index.js | 4edcb43fe7427753863338a2f3543ef9023fc7a1156143f707007cce62f7cd6c |

## Findings that change the smoke contract

| Source proof | Consequence |
| --- | --- |
| CLI bin91-95,129-144 selects profile mode for `web`; plugin management is a separate dynamic import. Profile wrapper1 delegates to the exact second boot chunk. | No plugin-install command is invoked by this dispatch. Do not add plugin/install arguments or overlays. |
| Web runtime index32 defaults `openBrowser=true`;174 suppresses it only for recognized SSH environment;194-205 launches the OS default browser after readiness. Startup22,42-45 parses `--no-open`; web patch137-144 wires that value. | Clean systemd launch lacks SSH markers. **Append `--no-open`** to the temporary instance argv; do not rely on opener failure, logs suppression or invented SSH environment. |
| Profile boot140-144 always rewrites selected `profiles/web/cordis.yml`;94-95,168 and243-245 read the selected home's optional home patch. | Prestart exact four-file pins remain mandatory. Poststart root-config hash equality to the preparation receipt is not an acceptance requirement: the source-derived rewrite is expected. The home-level patch must be absent before launch. No old-home patch merge is present in this chain. |
| App-boot409-437 traverses installed dependency/peer manifests and creates selected-home `profiles/node_modules` fallback links.371-388 can replace incorrect links. | Candidate must start with the verified fresh tree. Expected generated code links can be classified by metadata and trusted installed targets. Do not delete or reset them after smoke, and do not accept mutable-data redirects. The dependency traversal is runtime code behavior, not an authorization to inspect opaque data. |
| Profile boot220-280 mounts composition, installs shutdown hooks and watches the candidate profile/home patches; shutdown11,45-55 has five-second forced-exit bound. App-boot761-780 registers exact configuration watches. | No global PID/lock file or second bind is present in these inspected dispatch functions. Existing storage trace remains the evidence for per-home stores, in-memory jobs and read-only external skills. |
| Webserver imports node:http and no filesystem/network client module;102-106 keeps route/socket state in memory;197 creates one server;242-251 binds configured host/port;253-267 closes listener/connections on disposal. | No fixed Unix socket, global PID file or alternate-port listener in this server. One namespace-local `127.0.0.1:3081` bind is sufficient. Root HTTP can initially return404 before fallback registration; avoid an early request by a fixed bounded settling interval, without HTTP retries. |
| App-boot726-750 loads only cwd and selected-home dotenv layers, then inherited launch snapshot. Prior trace proves explicit telemetry DISABLED and empty pi-ai providers. | Fresh no-envfile checks and explicit clean environment remain necessary; no credential/UI/model discovery calls are needed. |

## Minimum temporary unit contract

Use one exclusive, root-created transient service, never `deepseek-harness.service`, with no persistent drop-in and no `JoinsNamespaceOf`. Invoke exactly the existing installed launcher plus `web --host 127.0.0.1 --port 3081 --no-open`.

- `User=dsh`, `Group=dsh`, `UMask=0077`, `WorkingDirectory=/srv/dsh/workspaces`, explicit clean `HOME=/home/dsh`, candidate `DSH_HOME=/home/dsh/.dsh-profiles/vm105-provider-v1`, preserved verified PATH; no envfile, credential variables, inherited interactive environment, socket activation or passed host sockets.
- `ProtectSystem=strict`, `ProtectHome=no`, `ReadWritePaths=/home/dsh/.dsh-profiles/vm105-provider-v1`, `InaccessiblePaths=/home/dsh/.dsh`, `PrivateTmp=yes`, `NoNewPrivileges=yes`. Workspace stays read-only. `ProtectSystem=full` was insufficient to make the write exceptions exclusive.
- `PrivateNetwork=yes` with a fresh network namespace. No veth, network attachment, host port forwarding or firewall mutation. Add `RestrictAddressFamilies=AF_INET AF_INET6` so filesystem Unix sockets cannot provide an alternative host-service connection. No inherited socket descriptors; stdin/stdout/stderr use null. These restrictions may expose a compatibility failure; do not relax them or retry in this attempt.
- `RuntimeMaxSec=120`, `TimeoutStartSec=30`, `TimeoutStopSec=15`, `Restart=no`, `MemoryMax=512M`, `CPUQuota=100%`, `TasksMax=128`, `KillMode=control-group`, `StandardInput=null`, `StandardOutput=null`, `StandardError=null`. Resource exhaustion ends the attempt; no automatic resource increase.

Systemd's strict filesystem protection supports explicit writable exceptions; read-only mounts alone do not block AF_UNIX sockets. PrivateNetwork creates a separate network namespace. Validate local systemd accepts and applies every required property; unsupported/ignored isolation is a blocking result, never a fallback. [Upstream systemd execution documentation](https://github.com/systemd/systemd/blob/main/man/systemd.exec.xml)

Read-only metadata check found `/usr/bin/nsenter`, `/usr/bin/systemd-run` and `/proc/self/ns/net`, current host namespace `net:[4026531840]`. This proves tool/path availability and a namespace interface, not successful namespace creation or applied sandboxing. Those are required runtime checks during the one attempt.

## Ordered acceptance and cleanup

1. Revalidate VM/service/tunnel identity, pilot PID/start/restart count and working loopback/tunnel HTTP. Revalidate installed source pins, exact four-file candidate bytes and ownership/modes, absence of candidate/cwd `.env` and home patch, exclusive absent transient-unit name, and executable trust. Record root-config prestart pin and expected source-derived rewrite separately. Do not read the old profile.
2. Create/start the one bounded unit. Capture its InvocationID, cgroup, MainPID/start identity and effective unit properties. Poll process/listener metadata for at most30 seconds with no HTTP requests. Allow a fixed five-second settling interval once the owned listener appears; it does not prove API readiness. Unit termination, ambiguity or timeout fails closed.
3. Verify actual listener inode/PID belongs to that cgroup, is `dsh:dsh`, has the exact candidate selector and argv, and its network namespace differs from the preserved pilot/host namespace. Require only loopback interfaces, no default/non-loopback routes and exactly the expected TCP listener in that namespace; no host namespace3081 listener was created. Verify all required sandbox properties. Inspect metadata only; never emit arbitrary environment values or profile contents.
4. Root opens/holds the verified candidate network-namespace descriptor and runs the bounded probe through `nsenter --net=... /usr/bin/python3.12 -I`, passing only that descriptor as needed. Recheck process start/cgroup identity immediately before the calls; do not accept a PID-reused namespace. Enter the network namespace only, never a shell login or the opaque profile. Require Python optimization level0.
5. One five-second root HTTP request, followed by the packet's exact `llm.providers` and `host.describe` readbacks using namespace-local port3081: one request each, five-second wall bound, no redirect/proxy/retry, at most64KiB per API body. Emit only sanitized pass/fail, zero-active count and expected-default equality. No browser rendering, login, OAuth, inference, model discovery or provider contact.
6. Stop the recorded owned unit on both success and failure; use bounded systemctl30-second subprocess timeout around its15-second stop bound. Verify inactive/absent, empty cgroup, no remaining candidate process and no3081 listener in the held namespace. Release the held namespace descriptor after verification. A retained descriptor is why the namespace can still be checked after stopping. Remove/collect only the owned transient unit state; no candidate deletion or pilot control operation.
7. Independently recheck unchanged pilot PID/start/restart count, loopback HTTP, tunnel process/listener/task and tunnel HTTP. Candidate generated-file inspection is metadata-only except expressly allowlisted source-generated configuration validation. Classify private ownership/modes and code-fallback symlink target trust; unexpected mutable links or external targets fail. Retain candidate files and record the new runtime inventory without rewriting the historical preparation receipt.

If creation/stop transport becomes ambiguous, inspect only the recorded unit/job/cgroup and finish its bounded stop; never issue a second startup. Failure to establish cleanup ends `CLEANUP_NOT_PROVEN` with no unrelated remediation.

## Generated fallback and metadata verifier amendment

Added 2026-09-09 00:31 Asia/Bangkok, using the already inspected app-boot source; no additional live reads or execution. This section makes step7 concrete. Build the expected installed closure **before startup**; validate the generated candidate tree only after the candidate is stopped and its cgroup is empty. A bounded manifest-driven traversal is permitted; a generic installed-directory crawl is not.

### Exact fallback algorithm to reproduce

App-boot409-437 starts `modulesDir = join(selectedHome, 'profiles', 'node_modules')`. It reads the installed CLI `package.json` at the canonical installation anchor. Its insertion-ordered `links` map first receives `appManifest.name -> dirname(installAnchor)`. Its FIFO queue first receives that anchor and manifest.

For each queued manifest, process `Object.keys(dependencies ?? {})` followed by `Object.keys(peerDependencies ?? {})`, preserving JSON declaration order. Count every encountered edge, including duplicates. If the package name is already in `links`, skip it: the **first successfully resolved occurrence wins**, even when a later dependency could resolve another version. Otherwise call `packageDirFromAnchor(currentAnchor, name)`. That function499-505 iterates `createRequire(anchor).resolve.paths(name)` in order and returns the first `join(searchPath,name)` whose `package.json` exists. It does not call a package installer or resolve package exports. If no directory resolves, runtime skips this name; unresolved names are not inserted. For a resolution, insert `name -> returned directory`, read that directory's `package.json`, and enqueue that manifest with its own manifest filename as the next anchor.

Finally, for every map entry, create `join(modulesDir, packageName)` as a symlink to the **returned path string**, after making its parent directories. Root CLI itself is included. Scoped names create exactly one `@scope` directory under `modulesDir`; unscoped names create none. `ensureSymlink`370-388 retains an existing link only when `readlink` exactly equals the expected target string; otherwise it replaces it. The audit must compare those exact strings, then independently validate their canonical targets. Comparing only realpaths is insufficient. Runtime replacement behavior is not permission for the verifier to correct anything.

### Bounded expected-map construction

- Maximum **256 package-name entries including root and1024 encountered dependency/peer edges**; each manifest at most1MiB; entire closure pass at most30 seconds. Exceeding any bound is BLOCKED without raising it. No installed JS execution is needed or authorized by this amendment.
- Start only from the already pinned canonical CLI manifest. For each dependency, reproduce Node's ordered ancestor `node_modules` lookup for that exact anchor filename, including the standard skip when an ancestor segment is itself `node_modules`. Preserve the logical anchor/path spelling used by the source; do not silently replace it with a canonical path before deriving the next search list. Validate every considered package name as one safe unscoped segment or exactly `@scope/name`; reject traversal, separators beyond the scoped pair, NUL and malformed dependency maps.
- This checker deliberately fails closed if no trusted match is found before lookup leaves `/opt/deepseek-harness`, or if resolution would require global/NODE_PATH search roots. Do not approximate Node's remaining global search order or claim that an unresolved installed-only lookup proves runtime would skip the package. This narrower verifier may block a harmless optional peer, which is an explicit acceptance limit rather than permission to guess or expand scope.
- Resolve only the exact manifest-directed candidates in search order. Missing candidates may be skipped after their applicable trusted parent/path metadata is checked. An existing malformed/untrusted candidate blocks; do not skip it in favor of another package. Follow only explicitly validated root-owned package links within the installation; require final canonical manifest and all canonical ancestors root:root, no group/world write, no forbidden ACL/capability, regular positive-link-count manifest, held nofollow descriptors, stable path/link metadata and double hashes. Check the manifest's `name` equals the requested name; keep its version as metadata, not a hardcoded0.1.1-rc.2 requirement for third-party dependencies. Do not evaluate exports, scripts or version constraints as code.
- Retain the resulting exact link-name/target map and each manifest hash plus logical/canonical mapping. Recheck held metadata and mappings at completion, then validate the same map after stopping the candidate. Any mismatch fails; never rebuild a different expected map to fit observed generated links.

### Candidate-tree metadata classification

Take one nofollow, descriptor-anchored candidate inventory after stop, at most4096 entries, depth16 and30 seconds. Reject mount/device transitions including same-device bind mounts, path/identity drift, ACL/capability attributes, special files, hard-linked regular files and every symlink outside the exact fallback map. Do not follow candidate symlinks while walking or read/hash arbitrary generated regular-file content.

- Candidate directories must remain `dsh:dsh`0700; ordinary regular files must be `dsh:dsh`0600 with link count1. Record only containment, relative path, type, owner, mode, size and stable identity metadata. Unknown regular files/directories meeting these rules are `PRIVATE_UNCLASSIFIED_METADATA`, not evidence of credentials, settings, cache purpose or any semantic readiness. Such classification is eligible for this smoke's **containment-only** state check; any later acceptance condition requiring a known semantic purpose stays NOT PROVEN. Unexpected type, location, permissions or identity is BLOCKED.
- Under `profiles/node_modules`, require **exactly** the expected link names plus the necessary scope-parent directories, with no extra regular files, nested directories or symlinks. Each link must be dsh-owned, single-link, have no forbidden attributes, and match the exact source-derived target string. Symlink0777 is normal and is not the ordinary-file0600 rule. Validate the trusted installed canonical target and ancestors without traversing its contents; its mapping must remain identical to the retained closure proof. Root installation link and third-party package links are allowed only if actually present in the expected map, never merely because they point somewhere under `/opt`.
- The four original candidate files retain their separate policy: package manifest, workspace file and override patch must match their reviewed expected bytes; `profiles/web/cordis.yml` is the explicitly known source-rewritten root and may be validated against the already inspected CLI constant, with its own runtime receipt. Unexpected original-file changes fail. Do not read any newly generated potential credential/settings file to identify its purpose.

Unknown/malformed/over-bound/external mappings, missing or extra fallback links, or incomplete metadata proof end BLOCKED. Preserve all candidate files and do not repair, delete, relax rules or repeat startup to force acceptance. Namespace/unit cleanup still runs according to step6 regardless of metadata verdict.

## Acceptance scope

Successful execution may establish `ISOLATED_CANDIDATE_SMOKE_PASS`: API mounting, zero active adapters, expected dormant selection and contained generated state. It does not prove ordinary service sandbox compatibility, owner-tunnel candidate access, selector cutover, credentials, usable provider routing, inference or full-v1 completion. Existing pilot and scheduled tunnel remain operational. The choices to retain the pilot or later perform the separately reviewed owner-ready cutover remain unchanged. Independent review of the actual execution helper and parent acceptance are still pending.
