# VM105 OmniRoute final-client review packet

Prepared 2026-09-13. This packet is non-executing and does not transfer the final-client root, gateway, credential, service, profile, provider, model, firewall, or default-routing authority.

The Linux production-launcher continuation starts from accepted source baseline `b6b0d6524b1fc040a37448e331589d41a45c4b1b`.

## Accepted anchors

- The one-shot systemd containment smoke passed with five assertions and unchanged `deepseek-harness.service`; receipt `docs/evidence/vm105-omniroute-containment-smoke-20260913.json` SHA256 `a5bdc12c4f2833671ba1c869578523b37e034d0826c06acd87620e9c42985920` at commit `4317d60b164517e267e35eb4648012ece7133c86`.
- Compatibility evidence `auto-switch-vm105-final-client-compatibility-20260913.md` SHA256 `9ad832f89e579504a806698614b78701162115b5bbf5929caad91a5e03093995` binds the native credential-pipe shape and two-request lifecycle.
- The accepted compiled-package pipe fixture SHA256 is `7d484038930e3f20ad8bbf337f32a05f8748bfc0fecff7f8a51c9fa2bc0ae22d`.
- Installed identity anchors remain `@deepseek-ai/dsh@0.1.1-rc.2` and `@deepseek-ai/dsh-client-ui-settings-models/lib/client.js` SHA256 `c3b9a2d2d074c600c10553a4b15e294082f5851dd4a834d06ddb268b310d18de`.

## Installed copied-module closure

The final 2026-09-13 strict read-only capture is **PASS**: 447 reachable packages, 10,026 modules, two accepted entrypoints, three selected config bundles, and seven Node runtime libraries. The current receipt is `docs/evidence/vm105-final-client-runtime-manifest-20260913.json`; its independently recomputed canonical manifest SHA-256 is `4331e0e5fd9ac6f5e881a0dae941f9f07dea7b69969ee8b610071ce14cb03f8b`, and its file SHA-256 is `54d117c638335edeefe43aaef0f181ea5317f7938a6861a145871a0d9e45e8dd`. This supersedes the historical 28-package/56-file source anchor and the earlier BLOCKED capture receipts.

The inspector binds accepted CLI/client entrypoint and config-bundle hashes, resolves reachable package roots within `/opt/deepseek-harness/node_modules`, and inventories regular `.js`, `.cjs`, `.mjs`, `.json`, `.node`, and `.wasm` files across those roots. Every hash compares pre/post path and held-descriptor identity; a second complete inventory must match before PASS. The accepted successor topology receipt `docs/evidence/vm105-dsh-topology-capture-successor-20260913.json` adds the exact 447-package, 2,029-link pnpm closure and headless patch SHA-256 `534dc49c84b0fb9c2d3278dc8990f577d57b7a88442941444b58c96ac1a09ba0`. Its raw file SHA-256 is `86882398a06921bd351c84dbfc1eecc9abeb963912df41499ee21462b50cb47f`; its canonical self-hash is `cc7ca73f74bd1e515416cbff65809531fff2c052d0f7e406b9cc2efdb4c1ab20`. This proves installed topology only. No copied final root, reconstruction, package execution, client, provider, or model ran.

Four separately authorized one-shot topology attempts are spent. The first stopped locally at `ACCEPTED_BUILDER_HASH_MISMATCH`; the second stopped locally at `ACCEPTED_PACKAGE_IDENTITY_INVALID`; the third reached remote source entry and preserved the signed `CAPTURE_TARGET_MISMATCH` receipt with raw SHA-256 `86d878deb002c62cf69013717d38193248b7deed5b19a5a37c40e87de07c001d`. The fourth ran the independently reviewed fixed source once under handle `339`, returned `rc=0`, and published the accepted PASS receipt above. No attempt retried or fell back, and no capture authority remains.

The manifest is UTF-8 JSON with alphabetically sorted object keys and module rows sorted by `stagedRelativePath`; its canonical digest excludes `canonicalManifestSha256` itself. The Node pin is `/opt/node-v24.19.0-linux-x64/bin/node`, SHA-256 `bc17c508ffeed0ec622934f9b7fa72f8e78da65350e63c3eceb56fa688aa5e12`; canonical paths and loader/shared-library hashes are present in the receipt. The install-path version is not a JavaScript-executed version probe. This captures installed sources. Production copying, symlink-aware group-readable sealing, descriptor custody, and before/after staging verification now have source implementations, but they have not run on Linux and remain unproven.

## Exact two-request transport

The final contained client performs one native generation step with tools disabled and `maxRetries: 0`. The coordinator supplies the real gateway key as one canonical UTF-8 `v1-refs` JSON document, `{"refs":{"<bound-reference>":"<value>"},"version":1}\n`, through the read descriptor passed directly to `systemd-run --pipe` standard input. The service copies that bounded document once to the worker pipe and closes its writer before fork. The contained decoder rejects any extra key, bound-reference mismatch, noncanonical encoding, or unsafe value. The relay and HTTP authorization header necessarily use an immutable key string. The supported claim is only transient contained-process memory with no persistence in argv, environment, files, logs, receipts, or the retained root; no wipe or complete memory-erasure proof is claimed. `credentialExposed:false` describes only those named exposure surfaces.

DSH receives a separate pipe containing a fixed nonsecret dummy credential document. Its generated nonsecret patch uses `/proc/self/fd/<dummy-fd>` with `watch: false`, binds `llm-pi-ai` to the exact loopback relay origin and `agent/normal`, and disables retry, direct-provider, and tool-producing rows. The relay substitutes the real gateway authorization only on its two accepted outbound requests; DSH never receives that production key.

Only these requests are accepted:

1. `POST <BOUND_RELAY_ORIGIN>/v1/chat/completions` with `model: "agent/normal"`, streaming enabled, `tools: []`, `tool_choice: "none"`, and the bound task/run/turn/idempotency tuple. Virtual-route data is a separately bound chat-request field and is not claimed as part of the shared tuple.
2. Before any output is released, `POST <BOUND_RELAY_ORIGIN>/v1/agent-routes/events` with event `output_started`, the identical task/run/turn/idempotency tuple, HTTP 202, and `{accepted:true}`.

The receipt must show exactly two classified HTTP requests, tuple equality, ACK-before-output, `progressCommitted:true`, `resumeRequired:false`, and zero redirects, retries, discovery, catalog, direct-provider, tool, or second-generation requests. It omits credential, prompt, response, endpoint secret, environment values, and provider attribution.

This bounded headless run is an acceptance canary for one tools-disabled generation and its durable `output_started` acknowledgement. Its exactly-two-request relay is deliberately exhausted after that pair. It does not exercise or prove the post-default ordinary unattended DSH service, a normal persisted session across turns, tool dispatch, `tool_started` acknowledgement, or the service's behavior after a failed or interrupted turn. Passing this canary cannot be promoted as proof that the production default path is complete.

The accepted DSH source has a separate ordinary-session implementation path: `packages/core/session/src/omniroute-protocol.ts` creates and validates task/run/turn/idempotency/review state and emits the five routing headers; `packages/llm/llm-pi-ai/src/adapter.ts` attaches that metadata to the model request and posts lifecycle events to `/v1/agent-routes/events`; `packages/core/agent-loop/src/omniroute-lifecycle.ts` flushes the pending event before delivery, requires HTTP 202 with `{accepted:true}`, records acknowledgement, and flushes again; and `packages/core/agent-loop/src/agent.ts` places those barriers before model output release and tool execution. On a routed failure, that agent path records `resumeRequired` and stops before ordinary request recovery, so it does not automatically replay a model or tool step. The source-level report `docs/omniroute-client-step-report-20260904.md` records focused native-wire and reload fixtures for those semantics.

The accepted file manifest contains installed compiled packages for session, `llm-pi-ai`, and agent-loop code, but it does not establish that this ordinary-session path boots from the copied pnpm/headless topology or that the real post-default profile composes and selects it. There is no accepted executable integration receipt for post-activation ordinary unattended DSH. That lane still needs an exact reviewed profile/config binding plus live multi-turn, tool-lifecycle, durable-ACK, and no-automatic-replay acceptance.

The proven no-network smoke cannot carry HTTP. The selected production source uses one root transient service invoked with `systemd-run --wait --pipe`. The outer launcher passes a separately supplied credential read descriptor directly as standard input without reading or copying it; inside the unit, the bounded document is copied once to a close-on-exec pipe and its writer closes before fork. No complete memory-erasure proof is claimed. Arbitrary descriptor inheritance is not used or claimed. The unit parent retains host networking only to open exactly two sockets to the bound numeric action-time peer after the child reports namespace readiness. The worker enters private network and PID namespaces, raises loopback, then forks; namespace PID 1 alone creates a private mount namespace with fresh `/proc`, read-only `/var/tmp` and `/run`, and only its assigned runtime directory writable. It proves network/PID identities, closes descriptors outside its fixed allowlist, drops privilege to `dsh`, and receives chat and durable-ACK sockets in one fixed `SCM_RIGHTS` payload. This replaces the earlier whole-unit `PrivateNetwork=yes` assumption. The worker's sole IP listener remains namespace-local loopback and binds the exact numeric port from the accepted `relayOrigin`; an unavailable port or actual-origin mismatch stops before DSH. The supported isolation claim is `IP_TCP_NAMESPACE_ONLY`: Linux pathname `AF_UNIX` sockets remain reachable and are an explicit residual. The native documented gateway is `http://192.168.1.68:20128/v1`; the earlier invented gateway TLS binding is withdrawn. Public `https://ai.mysw.me/v1` currently lacks `/v1/agent-routes/events` in its fresh proxy matcher and is unusable for durable ACK without a separately authorized route addition and live proof.

Strict Bell-PC2 SSH control access to VM105 and existing strict SSH to VM1205 `belladmin@192.168.1.68` are control/review facts. Previously observed loopback management forwarding is not an active provider-data tunnel. SSH forwarding remains only a candidate separately reviewed secure transport; VM105-to-gateway SSH credentials, durable data forwarding, and encrypted provider traffic remain NOT PROVEN. Retain the native private-LAN HTTP origin without inventing TLS or changing any gateway.

`scripts/Invoke-VM105FinalClient.py` preserves the accepted fixture proof for two-request ordering on literal loopback HTTP, ACK-before-first-byte, tuple equality, third-request denial, and cleanup. All unsupported methods, including unknown verbs, share the failure/denial path and wake waiters; a denied GET between chat and ACK releases no chat output. ACK waits until successful upstream chat response headers prove chat arrival, then rechecks failure before sending. The adversarial regression starts ACK while chat is held before its upstream send and observes the gateway arrival order chat then events. Its production preflight still requires external endpoint/key-reference bindings and a complete PASS closure manifest whose canonical hash is recomputed and matched to an independently supplied expected hash, emits sanitized presence only, and never invokes the final client.

The source now also accepts one Unix `SCM_RIGHTS` handoff containing exactly two already-connected TCP descriptors in fixed chat then durable-ACK order. It checks the exact handoff payload and both peers against the coordinator-supplied action-time gateway IP and port, rejects a third descriptor or ancillary/truncation mismatch, and installs the accepted sockets directly into the same two-request relay without calling `connect`. Unsupported platforms fail before reading the control socket. Before output commit, rejection or protocol failure closes all accepted descriptors and releases no output; after commit, an additional request receives HTTP 403 without changing the successful transaction, and normal shutdown closes the descriptors. The host-supervisor/contained-launcher boundary is fixed in `docs/contracts/vm105-production-staging-and-socket-handoff-contract-20260913.md`: no host-network listener, no general proxy, and only a namespace-local loopback relay.

`ACCEPTED_LIVE_BINDINGS = None` keeps every shape-valid binding inert before credential-descriptor or operating-system access. The accepted topology source gate now binds canonical self-hash `cc7ca73f74bd1e515416cbff65809531fff2c052d0f7e406b9cc2efdb4c1ab20`, while raw receipt bytes are independently pinned to `86882398a06921bd351c84dbfc1eecc9abeb963912df41499ee21462b50cb47f`. A future complete live dictionary must use the exact successor receipt path and self-hash, fixed source root `/`, fixed staging root `/var/tmp/omniroute-dsh-client-final-20260913`, and accepted manifest path/hash. Because the live dictionary remains unbound, the source is pre-operating-system **NOT EXECUTABLE**. Unit names accept only `vm105-dsh-<32hex>.service`; all existing capability, hardening, namespace, runtime, receipt, and exact-path cleanup controls remain required.

The focused Windows suites cover the fail-closed live-binding gate, raw topology receipt hash, canonical topology self-hash, direct credential-descriptor stdin, exact systemd command, strict credential decoding, separate dummy-credential and patch pipes, DSH invocation shape, topology reconstruction and symlink-aware sealing, private network/PID/mount ordering, fresh `/proc`, read-only `/var/tmp` and `/run`, exact compact service receipts, descriptor/privilege-drop order, exactly two numeric-peer connections and one `sendmsg`, bounded cleanup, live receipt identity, exact-path cgroup proof, plus the accepted transport and topology-capture tests. The concrete source adapters remain inaccessible behind the unbound live gate. Native DSH boot and every Linux execution behavior remain mocked and **NOT PROVEN**.

On any failure before output commit, release no output, record explicit resume state, terminate and reap the exact unit cgroup, close only owned descriptors, retain the final root and sanitized receipt, and stop without retry or recursive cleanup. The locked output commit is irreversible; later denied requests do not rewrite the completed transaction.

## Active real-service selectors

A 2026-09-13 sanitized read-only classifier inspected only allowlisted selector names and known-path equality. Receipt `docs/evidence/vm105-real-service-selector-classification-20260913.json` has SHA256 `5b1ffd4006834961d3810ce204b5e40cff9274971e013403c377fbad90779721`; it emitted no environment value or config content. The active `deepseek-harness.service` process has both `DSH_HOME` and `HOME`; `DSH_HOME` matches the existing pilot root `/home/dsh/.dsh`, and `HOME` matches `/home/dsh`. This binds the current service configuration root for a later rollback receipt. It does not authorize reading or changing the opaque pilot.

Source precedence is configured `dshHomePath`, then nonblank `DSH_HOME`, then OS home plus `.dsh`. The relevant default selector is `/settings.json#/agentDefaultModel`, with profile composition under `/profiles/web/cordis.yml` and its patch input. The exact active default-model value and profile composition remain **UNBOUND**: the fixed settings probe returned no readable result, and the two `dshHomePath` key-presence probes were false. No value was emitted.

A later default activation transfer must therefore name only: the existing service unit `/etc/systemd/system/deepseek-harness.service`; the current rollback selector `DSH_HOME -> /home/dsh/.dsh`; the exact new profile root; the exact non-secret selector/drop-in permitted to change; one restart; one rollback; and post-change real-client acceptance. It remains separate from isolated final-client acceptance.

## Remaining release bindings

1. Independently review the topology-bound launcher, then separately authorize and prove descriptor-held reconstruction, copied pnpm links, headless patch, entrypoint/config-bundle pins, Node/runtime hashes, symlink-aware sealing, and cleanup. The accepted installed topology does not establish a runnable copied final root.
2. Bound private loopback relay origin, the documented native HTTP LAN origin, and any separately reviewed secure-transport binding.
3. Credential key identity/reference and exact version-1 document schema, without the key value.
4. Bind the complete non-secret live dictionary, then live-accept the concrete decoder, dummy-credential adapter, DSH entry, fixed launcher/relay source, and receipt schema under the systemd controls, including real Linux DSH boot, `systemd-run --pipe` credential delivery, parent/worker namespace separation, descriptor transfer, privilege drop, runtime-directory removal, and exact-path cgroup cleanup.
5. Bind and independently accept the separate post-default ordinary-service profile/config path, then prove live routing metadata, multi-turn and tool lifecycle acknowledgements, durable persistence, and no automatic replay before coordinator acceptance of the exact final root and contract and fresh VM105 service/pilot state immediately before release.

Until all five are bound, absence of `/var/tmp/omniroute-dsh-client-final-20260913` must be freshly verified and this packet grants no execution or default-routing authority. Linux DSH boot and end-to-end behavior remain **NOT PROVEN**.
