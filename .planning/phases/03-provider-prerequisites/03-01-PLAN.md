---
phase: 03-provider-prerequisites
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/evidence/phase-03-provider-prerequisites.md
  - docs/evidence/phase-03-firewall-packet.json
  - docs/evidence/phase-03-firewall-review.md
  - docs/evidence/phase-03-firewall-results.json
  - scripts/Test-Phase03FirewallPacket.ps1
  - scripts/Test-Phase03Evidence.ps1
autonomous: false
requirements: [FW-01]
must_haves:
  truths:
    - "Every requested route has a freshly verified prerequisite record, or an explicit blocker that prevents enabling it."
    - "Only an individually proven route can receive an exact source/destination/protocol/port firewall permission with a verified inverse operation."
    - "Harness remains reachable through the owner SSH tunnel and unreachable directly on LAN TCP 3080."
  artifacts:
    - path: docs/evidence/phase-03-provider-prerequisites.md
      provides: "Secret-free route inventory, exact network decisions, baselines, rollback commands, and FW-01 verdict"
  key_links:
    - from: docs/evidence/phase-03-provider-prerequisites.md
      to: "Verified VM105 source and each verified provider listener"
      via: "Timestamped transport probes and exact firewall before/after comparison"
---

<objective>
Prove and prepare the network prerequisites for OpenRouter Auto, OpenRouter Pareto Code, VM1201 Qwen, Bell-PC2 local worker, Typhoon Thai text, Typhoon OCR, and Codex Luna/Terra/Sol native OAuth. Enable only justified narrow network access; provide the verified entry/storage inventory consumed by 03-02.
Purpose: Satisfy FW-01 without guessing endpoints, inheriting Hermes state, or broadening Harness exposure.
Output: docs/evidence/phase-03-provider-prerequisites.md. This evidence repository contains no application implementation; changes to installed Harness code, packages, worker runtime/model configuration, VM power, or architecture are outside this plan.
</objective>

<execution_context>
@C:/Users/chatc/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/chatc/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@docs/handoffs/2026-09-08-vm105-roadmap-continuation.md
@docs/evidence/vm105-deployment-2026-08-23-redacted.md
@docs/superpowers/specs/2026-09-08-vm105-orchestration-environment-design.md

Known historical interfaces, requiring revalidation: SSH user dsh; host 192.168.1.139; key C:/Users/chatc/.ssh/codex-prox01-vms-ed25519; service deepseek-harness.service; Harness 0.1.1-rc.2; /usr/local/bin/dsh; /opt/deepseek-harness; state directory /home/dsh/.dsh (0700); Web listener 127.0.0.1:3080. Exact provider configuration files, OAuth surfaces, model IDs, local-worker addresses and ports are not yet proven. Discover them read-only before producing a command that names a mutable target. The historical mention of VM1201 TCP 8000 is not authority to add a rule.

Discovery: existing deployment pattern plus bounded prerequisite inspection; generic research is disabled. There is no CONTEXT.md, RESEARCH.md, codebase map, or prior GSD SUMMARY. Use installed CLI help/package documentation and current official provider documentation during execution; do not install dependencies. The scope excludes Phase 4 inference, invalid-credential, and automatic-attribution tests.
</context>

<tasks>
<task type="auto">
  <name>Task 1: Build the current route and entry-surface inventory without secrets</name>
  <files>docs/evidence/phase-03-provider-prerequisites.md; docs/evidence/phase-03-firewall-packet.json; scripts/Test-Phase03FirewallPacket.ps1; scripts/Test-Phase03Evidence.ps1</files>
  <read_first>AGENTS.md; .planning/STATE.md; .planning/REQUIREMENTS.md; docs/handoffs/2026-09-08-vm105-roadmap-continuation.md; docs/evidence/vm105-deployment-2026-08-23-redacted.md</read_first>
  <action>Work only in the vm105-authoritative-roadmap worktree on codex/vm105-authoritative-roadmap; verify origin is https://github.com/chatchawan-simplewish/sw-localai-deepseek-harnes.git and preserve dirty files. Revalidate VM105 identity, service, listener, UFW defaults/rules, owner tunnel and direct LAN denial with read-only commands. Use BatchMode SSH with the recorded key and established host-key verification; never disable host-key checks. Inspect nonsecret installed Harness help/schema/documentation and the unauthenticated onboarding/settings UI to determine supported provider, custom endpoint, write-only credential, and native OAuth surfaces. For every browser project tab/link, first verify exactly Codex-Chrome-Bell-PC2; stop browser work if unavailable. No login, authorization consent, credential field entry, credential file contents, environment dumps, full service environments, or unfiltered logs. Inspect storage ownership/modes and path names only. Verify current official provider endpoints/model IDs and provider authentication method, plus actual VM1201/Bell-PC2 listener and firewall ownership through read-only host evidence. Record nine individually named rows: OpenRouter Auto; OpenRouter Pareto Code; VM1201 Qwen; Bell-PC2 local worker; Typhoon Thai text; Typhoon OCR; Codex Luna; Codex Terra; Codex Sol. Each row records source date/provenance, explicit provider/model, endpoint, auth method, supported Harness surface, exact state path when established, target resource owner, transport probe and verdict. Sharing one provider credential across that provider's documented routes is allowed only with explicit VM105 isolation; do not invent one credential per model. An unavailable route or undocumented native integration becomes BLOCKED with the exact missing fact, never an API-key substitute or copied Hermes OAuth. Keep explicit routing and record automatic routing disabled.
  Before any firewall mutation, additionally emit docs/evidence/phase-03-firewall-packet.json as the immutable review input. Record capturedAt, VM105 hostname, current address, MAC or machine identity correlated through existing verified SSH access, SSH host-key identity, baseline listener/service/tunnel/direct-port observations, and nine route records. Each route has either a supported no-change decision with existing-path proof, BLOCKED reason, or one exact proposed permission. Each permission records unique id, route, actual source, destination, protocol, port, purpose, resource owner, direction, exact baseline rule identity/set, exact add command, exact inverse command targeting only the new uniquely identified rule, exact allowed-source and unauthorized-source probe commands with expected exit/output, exact rule-state/postflight/rollback commands with expected results, and evidence that its destination listener is the intended service. Collect pre-change probe observations now; do not add a rule. Verify source identity as seen by the destination and preexisting authorization of the negative-probe source; do not treat an authorized source as the negative control. Store no secret-bearing commands. Implement scripts/Test-Phase03FirewallPacket.ps1 using PowerShell/.NET only, with -Stage Preflight|Postflight|Rollback and -RuleId for the latter stages. Preflight must reject unresolved/empty fields, malformed addresses/ports, any-source/subnet grants, source/destination mismatch against inventory, TCP 3080, missing add/inverse/probe commands, inverse targeting a baseline rule, duplicate rule identity, and baseline records lacking timestamps/provenance. Validate the exact add and inverse syntax against the discovered firewall platform and tuple rather than accepting arbitrary strings. For zero additions, require every route's existing-path/no-change evidence and do not manufacture a rule. Postflight/Rollback validate the results-file receipts described in Task 3 against the unchanged packet, not self-reported PASS labels. Include a -SelfTest switch using synthetic in-memory packets to reject a broad source, a mismatched inverse, absent rejected-source receipt, altered packet digest and before/after rule drift. No new package dependencies.
  Create scripts/Test-Phase03Evidence.ps1 using PowerShell/.NET only. Its fixed seven-file allowlist is docs/evidence/phase-03-provider-prerequisites.md, docs/evidence/phase-03-firewall-packet.json, docs/evidence/phase-03-firewall-review.md, docs/evidence/phase-03-firewall-results.json, docs/evidence/phase-03-provider-credentials.md, .planning/phases/03-provider-prerequisites/03-01-SUMMARY.md, and .planning/phases/03-provider-prerequisites/03-02-SUMMARY.md. It scans every existing allowlisted file in every invocation; -Stage Network requires the five network evidence/summary outputs, while -Stage Complete requires all seven. Reject missing stage-required files and do not offer exclude or ignore switches. Line-by-line, detect nonempty generic key/api_key/token/secret/password/client_secret assignments in text/JSON, provider-key formats including sk-, sk-or-, sess- and known documented provider prefixes, Authorization/Proxy-Authorization header values, private-key PEM blocks, and raw or percent-encoded OAuth code/state/access_token/refresh_token/id_token URL parameters. Credential labels with no values are not secret assignments; redact discoveries by removing the value entirely, never by hiding it from the scanner. Report only allowlisted relative filename, one-based line and category, with nonzero exit when findings/missing files exist; do not print matches, source lines, captured groups or exception payloads. Build an in-memory -SelfTest covering every detection category, harmless credential-free prose, missing required output, and value-suppressed reporting. No real secret fixtures. Run scanner plus manual redaction review before staging or claiming any Phase 3 plan completion, including summaries created at the end; every later output edit requires rerunning the scan.</action>
  <acceptance_criteria>All nine route rows exist with primary/installed evidence or a named BLOCKED reason; no unresolved field is treated as a live target. A concrete immutable packet, passing firewall validator and value-suppressing fixed-allowlist scanner exist before review. VM105 identity, SSH, loopback service and direct denial are freshly checked. No persistent provider access, firewall permission or secrets were created/read/entered.</acceptance_criteria>
  <verify><automated>PowerShell: ./scripts/Test-Phase03FirewallPacket.ps1 -SelfTest; if ($LASTEXITCODE -ne 0) { throw 'Firewall validator self-check failed' }; ./scripts/Test-Phase03FirewallPacket.ps1 -Stage Preflight; if ($LASTEXITCODE -ne 0) { throw 'Firewall packet invalid; review and mutation blocked' }; git diff --check -- docs/evidence/phase-03-provider-prerequisites.md docs/evidence/phase-03-firewall-packet.json scripts/Test-Phase03FirewallPacket.ps1</automated><human-check>Parent compares each recorded endpoint/model and storage path to its cited current source; a document-presence check alone does not prove support or connectivity. Task 1 cannot approve its own packet.</human-check></verify>
  <rollback>No live mutation is permitted in this task. Retain existing credentials, rules, service and tunnel. Identity drift, secret exposure risk, unsupported integration, or missing read-only access stops the affected lane before any dependent action; record BLOCKED and the precise required input.</rollback>
  <done>The inventory distinguishes discovered facts, transport reachability, and still-unproven Phase 4 inference.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-independent-review">
  <name>Task 2: Independently review and accept the immutable firewall packet</name>
  <files>docs/evidence/phase-03-firewall-review.md</files>
  <read_first>docs/evidence/phase-03-firewall-packet.json; docs/evidence/phase-03-provider-prerequisites.md; scripts/Test-Phase03FirewallPacket.ps1; AGENTS.md</read_first>
  <action>Pause all firewall mutation. The controller assigns a fresh independent reviewer who did not prepare the packet; the reviewer inspects every tuple, service/source identity, before-state, exact add/inverse command, positive/negative probe and validator behavior. The reviewer must run Preflight and SelfTest and identify any missing or high-severity finding. Record packet SHA-256 (safe nonsecret document digest), review identity, command results and findings. Only after all blocking findings are corrected does the controller explicitly accept that exact digest and list the exact approved rule IDs in this review record. Packet-author self-approval, a generated APPROVED marker, timeout or auto_advance cannot satisfy this gate. Any packet change invalidates acceptance and returns to independent review. This checkpoint is fulfilled by the independent reviewer plus controller acceptance under the owner's standing routine authority; do not ask the owner for a second routine firewall confirmation.</action>
  <what-built>Immutable per-rule packet, baseline proof and automated field/command/receipt validation. No firewall mutation has occurred.</what-built>
  <how-to-verify>Independent reviewer validates every proposed rule and inverse against discovered platform syntax, source/listener evidence and exact probes. Controller records acceptance of the same digest only with zero blocking findings. A no-addition packet also requires review of all unchanged-path decisions.</how-to-verify>
  <resume-signal>Controller acceptance naming packet SHA-256 and approved rule IDs, supported by the independent review record.</resume-signal>
  <acceptance_criteria>The independent review record has zero unresolved blockers, exact approved digest/rule IDs and explicit controller acceptance before any add command.</acceptance_criteria>
  <verify><automated>PowerShell: ./scripts/Test-Phase03FirewallPacket.ps1 -Stage Preflight; if ($LASTEXITCODE -ne 0) { throw 'Packet validation failed' }; Get-FileHash -Algorithm SHA256 docs/evidence/phase-03-firewall-packet.json; git diff --check -- docs/evidence/phase-03-firewall-review.md</automated><human-check>The controller compares the digest and accepted IDs to the genuine reviewer result. File existence or text alone is never approval. Task 3 must consume this verified controller acceptance.</human-check></verify>
  <rollback>No live mutation at this checkpoint. Failed review or digest mismatch returns to Task 1 preparation; preserve live state and do not weaken the rule or validator to bypass a finding.</rollback>
  <done>Independent review and controller acceptance have released exactly the prepared firewall packet for Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Apply approved rules individually and verify exact postflight receipts</name>
  <files>docs/evidence/phase-03-provider-prerequisites.md; docs/evidence/phase-03-firewall-results.json; exact live firewall targets in the independently accepted packet</files>
  <read_first>docs/evidence/phase-03-provider-prerequisites.md Task 1 inventory; docs/evidence/phase-03-firewall-packet.json; docs/evidence/phase-03-firewall-review.md; scripts/Test-Phase03FirewallPacket.ps1; .planning/PROJECT.md Constraints; docs/handoffs/2026-09-08-vm105-roadmap-continuation.md Safety boundaries</read_first>
  <action>Require the genuine Task 2 independent review/controller acceptance, exact current packet digest and approved IDs before any add command. Rerun Preflight and recheck current host/source/destination identities and firewall baseline; drift invalidates acceptance and stops the lane. Run only the packet's exact approved command for one rule, with no manual broadening or platform substitution. Public HTTPS paths already allowed need no new inbound rule; preserve all no-change decisions. In docs/evidence/phase-03-firewall-results.json record packet digest, rule ID, current identity/before-state, timestamp, exact executed command identifier and exit result, parsed actual after-state rule tuple/identity, retained baseline rules, actual allowed-source probe source/exit/status, actual rejected-source probe source/exit/status, and VM105 management/listener/tunnel/direct-port results. Execute the packet's exact bounded postflight commands and validate their observed values against expected values using -Stage Postflight -RuleId; receipts must come from those command executions, not manually written PASS labels. Assert after-state equals the baseline plus that single exact approved rule, accounting only for earlier accepted additions in this same packet; assert no preexisting rule changed and no extra rule appeared. Every permitted-source and denied-source probe must run from its recorded actual source. Do not proceed to the next rule until postflight passes. On failed postflight, run only the reviewed inverse if current identity and rule ownership still match; run exact rollback checks and -Stage Rollback -RuleId to assert prior policy, rule absence and restored management baseline. If inverse execution would touch an unowned/preexisting rule, stop without executing it. Keep explicit routing and do no inference; blocked prerequisite routes prevent phase acceptance.</action>
  <acceptance_criteria>Each supported route has either verified sufficient unchanged network access or exactly scoped added permission with before/after evidence, allowed-source and denied-source observations, exact inverse command and preserved management path. FW-01 is PASS only when the entire inventory is resolved and every addition satisfies these conditions. A missing prerequisite or unauthorized-source test leaves FW-01 BLOCKED or NOT PROVEN; no fabricated PASS.</acceptance_criteria>
  <verify><automated>PowerShell: ./scripts/Test-Phase03FirewallPacket.ps1 -Stage Preflight; if ($LASTEXITCODE -ne 0) { throw 'Packet changed or invalid' }; $packet = Get-Content -Raw docs/evidence/phase-03-firewall-packet.json | ConvertFrom-Json; $vm105Address = $packet.vm105.address; if (-not $vm105Address -or $packet.vm105.hostname -ne 'deepseek-harness-01' -or -not $packet.vm105.sshHostKeyIdentity -or -not $packet.vm105.correlatedMachineIdentity) { throw 'VM105 identity correlation missing' }; foreach ($rule in $packet.rules) { ./scripts/Test-Phase03FirewallPacket.ps1 -Stage Postflight -RuleId $rule.id; if ($LASTEXITCODE -ne 0) { throw 'Per-rule postflight failed; stop mutation and use reviewed rollback' } }; if (Test-NetConnection -ComputerName $vm105Address -Port 3080 -InformationLevel Quiet -WarningAction SilentlyContinue) { throw 'Forbidden direct 3080 exposure' }; $vmCheck = ssh.exe -i 'C:/Users/chatc/.ssh/codex-prox01-vms-ed25519' -o BatchMode=yes -o IdentitiesOnly=yes -o ConnectTimeout=10 "dsh@$vm105Address" 'hostname; systemctl is-active deepseek-harness.service; ss -H -ltn sport = :3080; curl --max-time 10 -s -o /dev/null -w "HTTP=%{http_code}\n" http://127.0.0.1:3080; sudo -n ufw status verbose'; if ($LASTEXITCODE -ne 0) { throw 'Read-only VM verification failed' }; $vmText = $vmCheck -join "`n"; foreach ($expected in @('(?m)^deepseek-harness-01$','(?m)^active$','127\.0\.0\.1:3080','(?m)^HTTP=200$','Status: active')) { if ($vmText -notmatch $expected) { throw 'VM baseline assertion failed' } }; $listeners = @($vmCheck | Where-Object { $_ -match '^LISTEN\s' }); if ($listeners.Count -ne 1 -or $listeners[0] -notmatch '\s127\.0\.0\.1:3080\s') { throw 'Listener is not exclusively loopback' }</automated><human-check>Run each Postflight immediately after that rule, then repeat the aggregate check after the final approved rule. For any reversal, run ./scripts/Test-Phase03FirewallPacket.ps1 -Stage Rollback -RuleId with that actual rule ID and require success. Revalidate SSH host-key and correlated machine identity before using the packet address; stale evidence or DHCP drift returns to Task 1 and independent review, never an ad hoc address replacement. Run bounded commands separately if their combined duration exceeds 60 seconds.</human-check></verify>
  <rollback>If an added rule fails its acceptance checks, execute only its pre-recorded exact inverse, provided current identity and before/after evidence prove the rule is newly plan-owned; this narrowly authorized reversal does not authorize deleting preexisting rules. Verify the original policy and SSH/tunnel/3080 baseline. If rollback needs a broader deletion, encounters drift, or fails, stop all live mutation, preserve evidence, report BLOCKED, and request exact owner direction. Do not stop/delete VMs, disks, credentials, models or services as a substitute rollback.</rollback>
  <done>FW-01 has a timestamped evidence-backed verdict; successful prerequisites can be consumed by 03-02 without any broader network permission.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| Bell-PC2 to VM105 | SSH identity, tunnel and loopback-only UI |
| VM105 to provider/worker | Source-scoped network access and verified endpoint identity |
| Live output to Git evidence | Only allowlisted nonsecret metadata may cross |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-03-01-S | Spoofing | Host, provider endpoint and browser identity | mitigate | Task 1 establishes correlated SSH identity, exact profile and primary-source endpoint provenance before review/mutation |
| T-03-01-T | Tampering | Firewall targets | mitigate | Task 1 exact packet/validator; Task 2 independent accepted digest; Task 3 verifies per-rule observed tuples/inverse and stops drift |
| T-03-01-I | Information disclosure | UI, logs and 3080 | mitigate | Task 1 no secret reads/entry; Task 3 preserves loopback and tests direct denial |
| T-03-01-D | Denial of service | Management/network access | mitigate | Sequential narrow additions, bounded probes, immediate baseline checks and exact inverse |
| T-03-01-E | Elevation of privilege | Destination provider permission | mitigate | Verify real source and unauthorized-source rejection; never generic ingress |
| T-03-01-R | Repudiation | Mutation evidence | mitigate | Timestamp provenance and exact before/after rule metadata |
| T-03-01-SC | Tampering | Package supply chain | accept | No installs or upgrades in this plan; a new package requirement stops the lane for audited replanning |

Apply ASVS L1 principles for authenticated management, least privilege, non-disclosure and secure transport. Any unresolved high-severity threat blocks mutation and PASS.
</threat_model>

<verification>
Parent verifies route evidence and exact firewall changes, including unchanged-path decisions. Confirm no native OAuth/API access creation and no model inference occurred; retain prior pnpm/RX-drop WARNs and VM104 current-need NOT PROVEN. Do not mark Phase 3 complete from network checks alone.
</verification>
<success_criteria>FW-01 is fully evidenced, no exposure expanded beyond justified tuples, rollback is executable and route prerequisites are concrete. Blocked routes remain explicit and prevent phase acceptance.</success_criteria>
<output>Create .planning/phases/03-provider-prerequisites/03-01-SUMMARY.md with secret-free evidence links, FW-01 verdict, route blockers and next credential-gate action. Before staging or claiming completion, run ./scripts/Test-Phase03Evidence.ps1 -SelfTest and ./scripts/Test-Phase03Evidence.ps1 -Stage Network, require exit zero, and complete independent manual redaction review of all five network evidence/summary outputs. Scan every existing file in the fixed seven-file allowlist, including any already-created credential outputs; missing required network outputs block completion. Rerun after every subsequent summary/evidence edit.</output>
