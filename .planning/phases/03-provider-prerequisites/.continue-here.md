---
context: phase
phase: 03-provider-prerequisites
task: 1
total_tasks: 1
status: paused_before_independent_plan_review
last_updated: 2026-09-08T18:40:57+07:00
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These constraints were discovered through actual failure. Acknowledge each one explicitly before proceeding.

- [ ] SCOPED-PATH PARSER: The accepted Task 1 parser rejects the installed wrapper handoff because one complete npm scope segment contains `@`. Do not broaden the existing plan or resume candidate preparation. First independently review the separate recovery plan at commit `77e39a4`.
- [ ] REVIEW TRUTH: Evidence cannot claim `ACCEPTED` before the independent review actually returns clean. Use changes-required metadata during fixes, then a reviewed metadata-only finalization.
- [ ] SSH ARGUMENT ORDER: Placing `--` after the SSH destination makes it the remote command and returns exit 127. Preserve all strict SSH options but place any terminator before the destination.

**Do not proceed until all boxes are acknowledged.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|----------------------|
| Silent parser widening | The installed scoped path did not fit the accepted grammar | blocking | Review and execute only `docs/superpowers/plans/2026-09-08-vm105-scoped-package-path-recovery.md`; allow `@` only as a complete conservative npm scope segment below `/opt/deepseek-harness` |
| Premature acceptance | Initial evidence claimed acceptance before external review | blocking | Record real changes-required state, fix and re-review, then make a separately reviewed metadata-only acceptance commit |
| Literal broken SSH example | The original example placed `--` after the destination | advisory | Keep strict SSH options and validate argv ordering with a read-only identity command before the bounded discovery runner |

<current_state>
Repository: `C:\Users\chatc\Projects\sw-localai-deepseek-harnes`

Authoritative worktree: `C:\Users\chatc\Projects\sw-localai-deepseek-harnes\.worktrees\vm105-authoritative-roadmap`

Repository URL: `https://github.com/chatchawan-simplewish/sw-localai-deepseek-harnes.git`

Branch: `codex/vm105-authoritative-roadmap`

Local HEAD at handoff creation: `77e39a4701ee3b6c5b3cfa8964049cc1e44b3115`

Phase 3 remains `01/02`; total remains `09/21`. The original reversible profile-pointer plan stopped after Task 1 produced an independently accepted `BLOCKED` selector record. Its Tasks 2 and 3 were never dispatched. A separate one-task recovery plan is committed but has not received independent plan review.
</current_state>

<completed_work>

- Approved specification: `docs/superpowers/specs/2026-09-08-vm105-reversible-profile-pointer-design.md`.
- Approved parent implementation plan: `docs/superpowers/plans/2026-09-08-vm105-reversible-profile-pointer.md`.
- Implemented typed selector evidence and validator through strict TDD and three reviewed fix rounds.
- Accepted BLOCKED evidence commit: `82ed70e63fdcc1b411ddc7221a1bb0019b1bd81b`.
- Parent verification: `SELF_TEST_PASS positive=6 negative=68`, `EVIDENCE_PASS stage=Selector`, Phase 3 Network exit 0, diff check exit 0.
- Evidence state: digest `8CC8BA324B8FBC4560542A048DD51C4617BDA0C95F4BB73FFE298201D5E2FCA4`; `overallVerdict=BLOCKED`; `selectorDiscovery.verdict=BLOCKED`; selector, blueprint, and candidate preparation are null; review is `ACCEPTED` with zero findings.
- Recovery-plan draft committed at `77e39a4701ee3b6c5b3cfa8964049cc1e44b3115`; one task, 10 PowerShell blocks parse, no placeholders, no implementation included.
</completed_work>

<remaining_work>

1. Verify exact origin, branch, local/remote heads, and dirty state. The handoff commit created after `77e39a4` may make the final task HEAD newer than the value above.
2. Independently review `docs/superpowers/plans/2026-09-08-vm105-scoped-package-path-recovery.md` against the parent plan, accepted blocker evidence, TDD, path traversal/shell safety, and the no-live-mutation boundary.
3. Fix and re-review any Critical/Important findings; keep plan provenance explicit.
4. Execute the accepted one-task recovery plan with a fresh implementer and a fresh independent task reviewer.
5. If and only if the resulting selector verdict is PASS, review status is ACCEPTED, reviewed digest matches, and Selector validation passes, resume the original preparation plan at Task 2. Otherwise stop before candidate creation.
</remaining_work>

<decisions_made>

- Candidate root stays fixed at `/home/dsh/.dsh-profiles/vm105-provider-v1`.
- The current profile remains untouched and opaque.
- A later service-pointer switch must be reversible, use one bounded maintenance restart, and automatically roll back the pointer on verification failure.
- Runtime cutover and credential/API/OAuth entry are separate future gates and are not authorized by this handoff.
- Routine development recommendations are pre-approved. Ask only for a supercritical, materially different, or materially more expensive departure.
- The recovery grammar may admit only a full lowercase npm scope segment matching `^@[a-z0-9][a-z0-9._-]*$` below `/opt/deepseek-harness`; arbitrary `@`, traversal, interpolation, shell syntax, symlinks, guessed paths, and broader discovery remain rejected.
</decisions_made>

<blockers>

- Installed wrapper target contains a complete npm `@scope` segment that the accepted parser rejects. This is a parser-contract blocker, not proof of a selector.
- No human action is currently needed. Credentials, provider login, service restart, pointer cutover, and profile creation remain unentered and unauthorized in this recovery stage.
</blockers>

## Required Reading (in order)

1. `AGENTS.md` — operating rules, authority, browser, Git, and rollover constraints.
2. `.planning/HANDOFF.json` — machine-readable current state and next action.
3. `docs/superpowers/plans/2026-09-08-vm105-scoped-package-path-recovery.md` — unreviewed recovery plan to critique before execution.
4. `docs/evidence/vm105-profile-pointer-preparation.json` — accepted BLOCKED selector record.
5. `scripts/Test-VM105ProfilePreparation.ps1` — implemented validator and self-tests.
6. `docs/superpowers/plans/2026-09-08-vm105-reversible-profile-pointer.md` — approved parent contract; Tasks 2–3 remain stopped.
7. `docs/superpowers/specs/2026-09-08-vm105-reversible-profile-pointer-design.md` — approved design boundary.
8. `.planning/STATE.md` and `.planning/ROADMAP.md` — authoritative evidence-task counts; note that STATE predates the selector-block implementation and must not override this handoff.

## Critical Anti-Patterns (do NOT repeat these)

- Do not treat an accepted BLOCKED record as selector PASS.
- Do not inspect, copy, move, overwrite, delete, or derive from current-profile contents.
- Do not create the candidate until selector PASS is independently accepted.
- Do not claim independent acceptance before the reviewer actually returns clean.
- Do not place raw installed source, unit environment, stderr, logs, secrets, tokens, or secret-shaped values in evidence or chat.
- Do not start credentials, provider login, OAuth, service lifecycle, systemd/drop-in, firewall, network, or cutover actions.

## Infrastructure State

- VM105: `deepseek-harness-01`, `192.168.1.139`.
- Service baseline last revalidated before this handoff: `deepseek-harness.service` active with zero restarts, listener `127.0.0.1:3080`, tunnel HTTP 200, direct LAN TCP 3080 denied, UFW unchanged.
- Current profile: untouched and opaque.
- Candidate profile: not created.
- Existing empty owner-only backup destination from prior Phase 3 work: `/home/dsh/.dsh/phase-03-provider-backup-20260908T110706`; no credential state was copied.
- Background processes started by this work: none.

## Pre-Execution Critique Required

- Design artifact: `docs/superpowers/plans/2026-09-08-vm105-scoped-package-path-recovery.md`
- Critique focus: exact npm-scope segment grammar; root boundary; traversal/interpolation/shell rejection; fixed-wrapper provenance; package identity/version; evidence digest and review truth; prohibition on live mutation.
- Gate: Do not begin recovery implementation until the plan receives an independent PASS or all Critical/Important findings are fixed and re-reviewed.

<context>
The shortest safe path is a one-task parser recovery, not a new profile mechanism. Preserve the accepted BLOCKED run as provenance. The recovery may update only `scripts/Test-VM105ProfilePreparation.ps1` and `docs/evidence/vm105-profile-pointer-preparation.json`, must use RED→GREEN, and must rerun only the fixed-wrapper read-only discovery. If selector semantics still cannot be proven, accept BLOCKED again and stop.
</context>

<next_action>
Start with: verify the repository URL, branch, HEAD, remote parity, and clean state, then dispatch an independent read-only review of `docs/superpowers/plans/2026-09-08-vm105-scoped-package-path-recovery.md`. Do not implement it before that review passes.
</next_action>
