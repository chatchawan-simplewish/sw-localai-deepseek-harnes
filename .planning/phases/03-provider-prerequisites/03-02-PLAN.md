---
phase: 03-provider-prerequisites
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - docs/evidence/phase-03-provider-credentials.md
autonomous: false
requirements: [CRED-01]
must_haves:
  truths:
    - "Every configured provider credential or native OAuth state is freshly established for VM105 and stored owner-only, without Hermes material."
    - "The owner sees concrete provider scope, safe storage metadata and rollback immediately before the first persistent access or sensitive entry."
    - "No unverified provider, model or automatic routing is silently enabled."
  artifacts:
    - path: docs/evidence/phase-03-provider-credentials.md
      provides: "Owner-gate record, safe credential provenance and storage-mode checks, rollback and CRED-01 verdict"
  key_links:
    - from: docs/evidence/phase-03-provider-prerequisites.md
      to: "Verified Harness write-only fields/native OAuth surfaces"
      via: "Exact per-route entry and storage packet approved at action time"
    - from: "Verified VM105 credential/native OAuth store"
      to: docs/evidence/phase-03-provider-credentials.md
      via: "Metadata-only ownership, permission, provenance and safe configured-state checks"
---

<objective>
Establish fresh isolated provider credentials and native Codex OAuth for the verified route inventory, with one concrete action-time owner confirmation and owner-only secret handling. Satisfy CRED-01; provider responses, invalid-key tests and child attribution remain Phase 4 work.
</objective>
<execution_context>
@C:/Users/chatc/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/chatc/.codex/get-shit-done/templates/summary.md
</execution_context>
<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@docs/handoffs/2026-09-08-vm105-roadmap-continuation.md
@docs/evidence/phase-03-provider-prerequisites.md
@.planning/phases/03-provider-prerequisites/03-01-SUMMARY.md

This plan consumes Task 1's exact verified provider/model/auth/UI/storage contracts; it must not infer them from old Hermes configuration. No package installation, integration adapter, API-key substitution for native OAuth, worker auth-policy change, inference load or service restart is authorized. All live provider changes have one sequential executor. Read-only reviewers never receive secrets. Runtime configuration paths must be resolved in the preflight packet before any write; repository evidence paths above are the only tracked outputs.
</context>

<tasks>
<task type="auto">
  <name>Task 1: Prepare exact owner-entry packet and reversible storage baseline</name>
  <files>docs/evidence/phase-03-provider-credentials.md; verified empty VM105 owner-only backup destination resolved from installed storage metadata</files>
  <read_first>docs/evidence/phase-03-provider-prerequisites.md; .planning/phases/03-provider-prerequisites/03-01-SUMMARY.md; AGENTS.md; .planning/PROJECT.md Constraints; docs/handoffs/2026-09-08-vm105-roadmap-continuation.md Open gates</read_first>
  <action>For each of the nine route rows, resolve the smallest supported credential/native OAuth scope, account/project safe identity, consumer, provider-issued nonsecret credential identifier when available, minimum practical lifetime, exact write-only/native entry surface, exact VM105 storage path, ownership and directory/file modes, and safe configured-state verification method. Shared provider access across documented models must be explicitly mapped. Confirm accepted FW-01 packet and service/tunnel baseline are still valid before any dependent operation; blocked routes remain blocked. Inventory preexisting storage by names/stat metadata only. Unexpected or provenance-unverified secret-bearing state blocks the lane: do not read its contents, copy, back up, export, quarantine, restore, relocate, delete or alter it. Do not inspect Hermes stores. Before owner approval, create only an empty host-local 0700 backup destination and metadata-only baseline; verify the destination remains empty and record absent target paths explicitly. No credential-state backup is allowed merely because its mode is secure. Plan exact host-local backup/restore commands for later use only after state is proven fresh and VM105-specific, with verified source/destination, 0600-or-narrower files and protected parents; evidence must contain no contents, secret hashes/lengths/fragments or OAuth state/code URLs. Record a reversible consumer disconnect that leaves credential material in place and does not delete or overwrite secret state. Restore is permitted only from a separately proven fresh VM105-specific backup; if no such backup exists, do not invent one or copy uncertain data to enable rollback. If the installed consumer cannot be disconnected safely without secret deletion, broad overwrite, shared/global identity change or service change, block the lane before entry. Revocation/deletion instructions remain informational and require separate owner authority. Prepare a finite packet with exact entry scope, sequence and post-provenance backup eligibility; no key creation, authorization consent or secret typing.</action>
  <acceptance_criteria>The packet identifies every exact supported entry/store path and scope or blocker; the protected backup destination is verified empty; no secret-bearing state was copied before approval or provenance proof; every proposed entry has a concrete consumer-disconnect rollback that preserves all existing material.</acceptance_criteria>
  <verify><automated>PowerShell: $p='docs/evidence/phase-03-provider-credentials.md'; $t=Get-Content -Raw $p; @('Entry packet','Storage baseline','Rollback','Owner gate','CRED-01') | ForEach-Object { if (-not $t.Contains($_)) { throw "Missing packet section: $_" } }; git diff --check -- $p</automated><human-check>Run each packet's exact bounded SSH stat/ACL check without reading content. Require runtime owner to match the verified Harness service user, directories 0700 and secret files 0600 or narrower, including actual backing store and backup paths; stop unsupported ownership or insecure parents/symlinks. Parent checks the completed packet before entry.</human-check></verify>
  <rollback>Before owner approval there is no credential backup to restore. Leave the empty protected destination in place if preparation stops; do not alter any uncertain state. Unexpected/provenance-unverified material is never copied, backed up, exported, quarantined or restored. Missing provider support or a safe consumer disconnect blocks the route and phase acceptance.</rollback>
  <done>The packet is concrete and reviewable; only the required action-time owner confirmation and owner interactive entry remain.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Obtain action-time confirmation and perform owner-only entry</name>
  <files>docs/evidence/phase-03-provider-credentials.md; exact VM105/provider storage targets listed in the approved Task 1 packet</files>
  <read_first>docs/evidence/phase-03-provider-credentials.md Entry packet, Storage baseline and Rollback; docs/handoffs/2026-09-08-vm105-roadmap-continuation.md Open gates</read_first>
  <action>Stop immediately before the first persistent API/OAuth access creation or sensitive credential entry. Present one action-time confirmation for the finite prepared entry session using the required timestamp and Overview, What I need from you, Choices, Recommended choice, Exact reply headings. Name verified providers/account-project safe labels, scopes, lifetime, storage paths, expected incremental cost, sequence and reversible configuration rollback; explain that the handoff explicitly requires this gate and standing routine approval does not replace it. Await explicit owner authorization; silence, timeout, auto_advance and prior planning approval do not authorize entry. After approval, the owner performs secret-bearing creation/typing and native authentication/consent in the already verified Codex-Chrome-Bell-PC2 profile using the installed supported surfaces, one provider at a time. The agent may operate only safe controls and metadata between entries. Do not request secrets in chat or tool arguments; no screenshot, DOM snapshot, terminal capture, clipboard read, network-body capture or log collection while secret-bearing pages/flows are active. The owner reports completion with nonsecret status only and navigates to a known safe configured-state page before observation resumes. Verify each provider's safe configured status before the next provider; keep provider/model selection explicit. This is one approval for the exact uninterrupted prepared session, not permission for added scopes, new services or a later resumed entry session; drift or additional cost stops the lane and returns to concrete preparation. Do not re-prompt for already authorized routine safe controls within the session. If local worker fresh credentials require changing its global/shared identity or auth policy, stop that route for scoped replanning rather than rotating existing consumers.</action>
  <what-built>Complete independently accepted route-specific network evidence, metadata-only baseline, empty protected backup destination, exact credential/native-OAuth entry packet and reversible consumer disconnect.</what-built>
  <how-to-verify>Owner first confirms the named finite access session. Owner then creates only the fresh VM105-specific access and enters it through the verified write-only/native flow; never provide the value to the agent. Existing working provider accounts and production identities remain unchanged. Return the UI to a safe status view after each entry.</how-to-verify>
  <resume-signal>Owner explicitly approves the prepared session, then reports only which provider entries completed or failed. A completion report without the pre-entry confirmation is not evidence that the gate was satisfied.</resume-signal>
  <acceptance_criteria>Timestamped approval precedes the first sensitive action; each issued access is fresh and VM105-specific; the agent never observes or records credential values or OAuth state; no route exceeds its reviewed scope.</acceptance_criteria>
  <verify><automated>PowerShell: $p='docs/evidence/phase-03-provider-credentials.md'; $t=Get-Content -Raw $p; if ($t -notmatch '(?m)^Owner gate: APPROVED at ') { throw 'Owner action-time approval not recorded; entry remains blocked' }; git diff --check -- $p</automated><human-check>Only the actual owner reply and safe provider-issued metadata establish approval/provenance. The automated marker check cannot grant permission. Missing entry/approval remains BLOCKED.</human-check></verify>
  <rollback>On rejection or no reply, leave entry untouched and stop at this checkpoint. On failed approved entry, stop further providers and use only the packet's safe consumer disconnect, leaving issued material in its existing owner-only store. Do not copy, back up, export, quarantine or restore any state whose fresh VM105-specific provenance is not established. Never revoke/delete access, expose a callback port or modify production identity as rollback. Report unresolved access for owner-controlled revocation later.</rollback>
  <done>Owner completes supported fresh credential/native OAuth entry for the prepared routes, or the exact incomplete routes remain explicitly blocked with no unauthorized workaround.</done>
</task>

<task type="auto">
  <name>Task 3: Verify secret-free storage isolation and record CRED-01 outcome</name>
  <files>docs/evidence/phase-03-provider-credentials.md</files>
  <read_first>docs/evidence/phase-03-provider-credentials.md approved Entry packet and Owner gate; docs/evidence/phase-03-provider-prerequisites.md; scripts/Test-Phase03Evidence.ps1; .planning/REQUIREMENTS.md</read_first>
  <action>After the owner exits secret-bearing screens, verify each approved route's safe configured indicator and expected explicit provider/model mapping. Through bounded metadata-only SSH checks, verify actual backing-store owner, permissions, protected parent/backup directories and non-shared target. Correlate newly issued nonsecret provider credential ID/creation metadata where available with owner completion and VM105 file creation/modification metadata; never hash, print or compare secret contents. Where fresh VM105-specific provenance cannot be independently established, record NOT PROVEN, block that lane and leave the state untouched: no copying, backup, export, quarantine or restore. Only after provenance passes may an exact packet-approved host-local backup be created, preserving 0700 directories and 0600-or-narrower files; record safe ownership/mode/timestamp metadata only. Confirm no inherited state was copied by the baseline and operation ledger; do not inspect Hermes. Codex Luna/Terra/Sol must map to actual native OAuth state rather than a substituted key route. Recheck service, tunnel, loopback listener, direct LAN denial and accepted firewall baseline from 03-01 using its identity-verified address variable. Write each credential/native state verdict, exact nonsecret rollback and separately owner-controlled revocation steps, limits and remaining Phase 4 tests. CRED-01 may be PASS only when all required provider stores and provenance meet the requirement; owner-deferred/unsupported routes prevent phase acceptance. Create the complete 03-02-SUMMARY.md draft before the final scan so summaries are included. Run the existing scripts/Test-Phase03Evidence.ps1 over its fixed seven-file allowlist, with -SelfTest and -Stage Complete; it must suppress values and detect generic assignments, provider key formats, authorization headers, private keys and OAuth parameters, and fail missing files/findings. Require this scan and independent manual redaction review before any Phase 3 staging or completion claim; later edits require another scan. Do not record credential/OAuth values in reports or commits, and do not run inference or invalid-credential tests.</action>
  <acceptance_criteria>Every required provider/native OAuth store is freshly evidenced and owner-only, or explicitly BLOCKED/NOT PROVEN. The record contains no sensitive values. Harness remains loopback-only; explicit routing remains selected. CRED-01 PASS never implies a Phase 4 route PASS.</acceptance_criteria>
  <verify><automated>PowerShell: ./scripts/Test-Phase03Evidence.ps1 -SelfTest; if ($LASTEXITCODE -ne 0) { throw 'Evidence scanner self-check failed' }; ./scripts/Test-Phase03Evidence.ps1 -Stage Complete; if ($LASTEXITCODE -ne 0) { throw 'Phase 3 evidence or summary scan failed; staging and completion blocked' }; $t=Get-Content -Raw docs/evidence/phase-03-provider-credentials.md; if ($t -notmatch '(?m)^CRED-01 verdict: (PASS|WARN|BLOCKED|NOT PROVEN)$') { throw 'Missing CRED-01 verdict' }; git diff --check -- docs/evidence/phase-03-provider-credentials.md .planning/phases/03-provider-prerequisites/03-02-SUMMARY.md</automated><human-check>Run exact stat/ACL and safe configured-state checks from the approved packet, verify real backing stores and safe provenance, and repeat the 03-01 SSH/listener/direct-denial checks. Parent manually reviews every fixed-allowlist evidence/summary output before staging or acceptance. Scanner success supplements, and cannot replace, manual redaction review.</human-check></verify>
  <rollback>If a changed provider fails storage or isolation checks, stop its use and all subsequent live changes; execute only its recorded safe consumer disconnect. Restore only from a backup independently proven fresh and VM105-specific and only when the target and packet-authorized inverse still match. Unexpected or provenance-unverified state stays untouched: no copying, backup, export, quarantine or restoration. If rollback verification fails, leave BLOCKED with exact discrepancy; no deletion or broad restoration. Retain all known WARN and NOT PROVEN findings.</rollback>
  <done>CRED-01 has a bounded evidence-backed verdict and Phase 4 has the exact explicit route inventory it can test. No Phase 4 result is claimed.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| Owner/provider to Harness | New API access or native OAuth entered only through verified protected surfaces |
| Harness to filesystem/backup | Credential and OAuth persistence is VM105-specific and owner-only |
| Runtime/browser to evidence | Secret values and OAuth state must never enter agent output or Git |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-03-02-S | Spoofing | OAuth/provider account and entry UI | mitigate | Exact verified profile/endpoint, installed native flow, owner-only consent; Task 2 blocks substitution |
| T-03-02-T | Tampering | Credential configuration | mitigate | Task 1 exact targets, metadata baseline and empty protected destination; Task 3 permits backups only after fresh VM105-specific provenance; uncertain state stays untouched |
| T-03-02-R | Repudiation | Owner authority and credential provenance | mitigate | Task 2 timestamped action-time confirmation, safe issuance metadata and entry ledger |
| T-03-02-I | Information disclosure | Secret entry, storage, backups and evidence | mitigate | No agent secret capture; 0700 directories/0600 files; metadata-only proof; allowlisted redaction review |
| T-03-02-D | Denial of service | Existing consumers | mitigate | Fresh dedicated access, no global worker identity rotation; safe consumer rollback preserves existing access |
| T-03-02-E | Elevation of privilege | Persistent provider access | mitigate | Least scope/lifetime and owner gate; no production identity mutation or inherited Hermes state |
| T-03-02-SC | Tampering | Package supply chain | accept | No package installation; unsupported surfaces trigger audited replanning, not new dependencies |

ASVS L1 applies to access control, secret protection and secure communication. Unresolved high-severity threats block entry and PASS; an automated checkpoint marker never substitutes for owner authority.
</threat_model>

<verification>
The parent verifies actual approval and each safe store/provenance check; repository text checks alone never establish CRED-01. Both FW-01 and CRED-01 must satisfy their full acceptance criteria before Phase 3 is accepted. Evidence-task counts do not advance simply because plans were written, and any later recorded blocked conclusion must remain distinguished from successful prerequisites.
</verification>
<success_criteria>Fresh VM105-specific owner-only credential/native OAuth prerequisites are established for all requested routes without secret disclosure or inherited material; otherwise the exact unresolved routes remain blocked. Network exposure and explicit routing are preserved.</success_criteria>

<source_audit>
| Source | ID | Item | Plan/task | Status |
| --- | --- | --- | --- | --- |
| GOAL | Phase 3 | Isolated owner-controlled prerequisites without inherited secrets or overbroad access | 03-01 tasks 1-3; 03-02 tasks 1-3 | COVERED |
| GOAL | Success 1 | Fresh owner-only credentials/native OAuth, no Hermes | 03-02 tasks 1-3 | COVERED |
| GOAL | Success 2 | Secret-free baseline and exact rollback before each live change; no backup of uncertain state | 03-01 tasks 1-3; 03-02 tasks 1-3 | COVERED |
| GOAL | Success 3 | Exact independently reviewed firewall tuple and rollback, no 3080 allowance | 03-01 tasks 1-3 | COVERED |
| REQ | FW-01 | Route-specific justified permissions | 03-01 tasks 1-3; requirements field once | COVERED |
| REQ | CRED-01 | Fresh VM105 owner-only provider credentials/native OAuth | 03-02 tasks 1-3; requirements field once | COVERED |
| RESEARCH | absent | Research disabled; no RESEARCH.md input | Bounded installed/official prerequisite verification in 03-01 task 1 | N/A |
| CONTEXT | absent | No CONTEXT.md or D-XX decisions | Roadmap, project and handoff constraints below | N/A |
| HANDOFF | entry gate | One concrete action-time owner confirmation before persistent access/secret typing | 03-02 task 2 | COVERED |
| PROJECT | safety | Sequential live changes, exact profile, no secrets, no VM/disk/credential/VM104-rule deletion, no archival, explicit routing | Both plans' tasks and rollback clauses | COVERED |
| PROJECT | scope | All nine named model/route variants retained; inference/failure/attribution owned by Phase 4 | 03-01 task 1; 03-02 task 3 | COVERED |

Dependency graph: 03-01 task 1 needs current deployment evidence and creates exact route/entry contracts, immutable firewall packet and validators; task 2 independently reviews that packet and records controller acceptance of its digest; task 3 consumes that exact acceptance, applies rules sequentially and creates observed network proof. 03-02 task 1 needs accepted route/network proof and creates the finite owner-entry packet with metadata baseline and empty protected backup destination; task 2 needs that packet and owner action-time confirmation and creates fresh access; task 3 needs completed entry, establishes provenance before any backup and creates CRED-01 verification. 03-01 task 2 pauses for independent reviewer/controller acceptance; only 03-02 task 2 requires owner interaction. Both final summaries are required inputs to the corresponding scanner stage and manual review before staging/completion. Each live-resource lane has one executor. No overlapping plan-owned files; waves remain sequential because reviewed live network proof precedes credential entry.
</source_audit>
<output>Create .planning/phases/03-provider-prerequisites/03-02-SUMMARY.md recording CRED-01 verdict, safe evidence links, remaining gates and exact Phase 4 next action. This file is part of the fixed seven-file scanner allowlist: run ./scripts/Test-Phase03Evidence.ps1 -Stage Complete after its final write and require exit zero plus independent manual redaction review of every evidence/summary output before staging or completion. Any later edits invalidate that scan. Leave predecessor tasks unarchived.</output>
